# Tarea 1: Operaciones con Vectores y Matrices
# 
# Crea dos vectores numéricos y realiza operaciones aritméticas entre ellos (suma, resta, multiplicación, división).
# Crea dos matrices y realiza operaciones como transposición, multiplicación de matrices y cálculo del determinante.
# Calcula la suma acumulada y el producto acumulado de un vector numérico utilizando funciones vectorizadas en R.
# Resuelve un sistema de ecuaciones lineales utilizando la función solve() y verifica la solución.


# Tarea 2: Análisis de Datos con Vectores y Matrices
# 
# Crea un vector con datos numéricos y realiza una exploración de datos básica, como cálculo de media, mediana y desviación estándar.
# Crea una matriz con datos aleatorios y realiza un análisis exploratorio, como identificar valores atípicos y visualizar los datos en un gráfico.
# Agrupa los datos en la matriz por categorías y calcula estadísticas resumen para cada grupo utilizando funciones de agrupación y resumen en R.


# Tarea 3: Álgebra Lineal y Descomposición SVD
# 
# Crea una matriz dispersa grande y resuelve un sistema de ecuaciones lineales utilizando la descomposición SVD.
# Calcula la matriz de autovectores y autovalores de una matriz simétrica y verifica que los autovalores sean iguales a los de la descomposición SVD.
# Utiliza la matriz de autovectores para realizar una transformación ortogonal de los datos y visualizarlos en un gráfico.
# Implementa una función para calcular la pseudo-inversa de una matriz mediante SVD


 
# Tarea 4: Manipulación Eficiente de Datos
# 
# Genera un conjunto de datos grande y utiliza el paquete data.table para realizar operaciones eficientes, como filtrado, agrupación y resumen estadístico.
# Utiliza la función dtypes() del paquete data.table para verificar los tipos de 
# datos en tu conjunto de datos y realizar conversiones si es necesario.
# Crea una matriz dispersa a partir de un tibble con datos dispersos utilizando el paquete sparsity.
# Resuelve un sistema de ecuaciones lineales con matriz dispersa utilizando el paquete superLU.